# Components

This directory contains React components for the Ghost Protocol web application.

## Structure

- `ui/` - UI components (buttons, cards, etc.)
- `features/` - Feature-specific components
- `layout/` - Layout components (header, footer, etc.)
